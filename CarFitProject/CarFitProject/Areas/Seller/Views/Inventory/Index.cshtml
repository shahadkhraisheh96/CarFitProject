@model CarFitProject.Helpers.PaginatedList<CarFitProject.Models.CarListing>
@{
    ViewData["Title"] = "My Listings";
}

<div class="container py-5">
    <div class="row align-items-center mb-4 g-3">
        <div class="col">
            <h2 class="fw-bold text-dark mb-1"><i class="fa-solid fa-car me-2 text-primary"></i>My Listings</h2>
            <p class="text-muted mb-0">Manage the vehicles you've submitted to CarFit.</p>
        </div>
        <div class="col-auto">
            <a asp-action="AddCar" class="btn btn-primary fw-bold">
                <i class="fa fa-plus me-1"></i>Add a listing
            </a>
        </div>
    </div>

    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success" role="alert">@TempData["SuccessMessage"]</div>
    }
    @if (TempData["ErrorMessage"] != null)
    {
        <div class="alert alert-warning" role="alert">@TempData["ErrorMessage"]</div>
    }

    @if (!Model.Any())
    {
        <div class="bg-white shadow-sm rounded-4 p-5 text-center">
            <i class="fa fa-car-side fs-1 text-muted opacity-25 mb-3 d-block"></i>
            <p class="text-muted mb-3">You haven't listed any vehicles yet.</p>
            <a asp-action="AddCar" class="btn btn-primary fw-bold">Add your first listing</a>
        </div>
    }
    else
    {
        <div class="bg-white shadow-sm rounded-4 p-0 overflow-hidden">
            <div class="table-responsive">
                <table class="table align-middle mb-0">
                    <thead class="table-light">
                        <tr>
                            <th class="ps-4">Vehicle</th>
                            <th>Year</th>
                            <th>Price</th>
                            <th>Status</th>
                            <th class="text-end pe-4">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td class="ps-4 fw-semibold">
                                    @(item.Car?.Make) @(item.Car?.Model) <span class="text-muted small">@(item.Car?.Trim)</span>
                                </td>
                                <td>@(item.Car?.Year)</td>
                                <td class="font-monospace">@(item.ListingPrice?.ToString("N0")) JD</td>
                                <td>
                                    @if (item.Status == "Active")
                                    {
                                        <span class="badge bg-success bg-opacity-10 text-success border border-success">Active</span>
                                    }
                                    else if (item.Status == "Pending")
                                    {
                                        <span class="badge bg-warning bg-opacity-10 text-warning border border-warning">Pending Review</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-secondary bg-opacity-10 text-secondary border border-secondary">@item.Status</span>
                                    }
                                </td>
                                <td class="text-end pe-4">
                                    <div class="d-inline-flex gap-2 flex-wrap justify-content-end">
                                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-outline-primary fw-bold">
                                            <i class="fa fa-pen me-1"></i>Edit
                                        </a>
                                        @if (item.CarId.HasValue)
                                        {
                                            <a asp-controller="Inspections" asp-action="Edit" asp-route-carId="@item.CarId" class="btn btn-sm btn-outline-info fw-bold">
                                                <i class="fa fa-clipboard-check me-1"></i>Inspection
                                            </a>
                                        }
                                        @if (item.Status == "Active")
                                        {
                                            <form asp-action="Deactivate" method="post" class="m-0"
                                                  onsubmit="return confirm('Mark this listing as sold?');">
                                                @Html.AntiForgeryToken()
                                                <input type="hidden" name="id" value="@item.Id" />
                                                <button type="submit" class="btn btn-sm btn-outline-warning fw-bold">
                                                    <i class="fa fa-ban me-1"></i>Mark sold
                                                </button>
                                            </form>
                                        }
                                        <form asp-action="Delete" method="post" class="m-0"
                                              onsubmit="return confirm('Delete this listing permanently?');">
                                            @Html.AntiForgeryToken()
                                            <input type="hidden" name="id" value="@item.Id" />
                                            <button type="submit" class="btn btn-sm btn-outline-danger fw-bold">
                                                <i class="fa fa-trash me-1"></i>Delete
                                            </button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>

        <partial name="_Pager" model="@(new CarFitProject.ViewModel.PagerViewModel { Page = Model.PageIndex, TotalPages = Model.TotalPages, Action = "Index" })" />
    }
</div>
